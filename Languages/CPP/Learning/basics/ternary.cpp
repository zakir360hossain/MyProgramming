#include <iostream>

using namespace std;

int main() {

        // (condition) ? true:false
        // Here if true, it will assign 5 to the variable
        int largestNum = (5>2) ? 5 : 2;
        cout << largestNum << endl;

        return 0;
}
