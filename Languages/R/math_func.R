x <- c(4.258,-3.8432, 5.4456, 7.456, 9.5605)

abs(x) # Absolute value of each element
ceiling(x) # Returns the next largest integer for each elem.
floor(x) # Returns the previous smallest integer for elem.
round(x)
round(x, 2) # To 2 decimals.
truc(x) # Trucates the decimals and returns the whole number.
exp(x) # e^elem
log(x)
log(x, base = 2)
log2(x) # Same as previous. And has many other bases.

x2 <- c(4, 3, 6)
factorial(x2) # for each elem
